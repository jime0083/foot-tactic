# foot-tactic 手動作業管理 (manual-work)

ルール:
- ここに記載のWorkはAIでは実行できない(人間のアカウント操作・決済・コンソール操作が必要な)作業
- 各Workには「期限」として、最悪どのPhase開始までに完了していれば開発を止めずに進められるかを記載
- AIは各Phase着手前にこのファイルを確認し、期限が到来しているWorkが未完了の場合は
  Phaseのタスクを進めず、未完了のWorkがある旨をユーザーに報告する(CLAUDE.md参照)
- 完了時は [x] と日付を記入する

=====================================================
Work1: Firebase / Google Cloud 初期セットアップ
=====================================================
[x] Work1.1 Firebaseプロジェクト作成とWebアプリ登録 (2026-07-19 完了・firebaseConfig受領)
    期限: Phase1.2 開始まで
    手順:
      1. https://console.firebase.google.com/ で新規プロジェクト作成(例: foot-tactic)
      2. プロジェクト設定 > 全般 > 「アプリを追加」でWebアプリを登録
      3. 表示されるWeb(apiKey, authDomain, projectId, storageBucket,
         messagingSenderId, appId)の値をAIに共有する(.envに設定するため)
    備考: 無料のSparkプランで開始可能(Cloud Functionsは使用しない構成)

[ ] Work1.2 Firebase AuthenticationでGoogleプロバイダを有効化
    期限: Phase1.4 開始まで
    手順:
      1. Firebaseコンソール > Authentication > ログイン方法
      2. 「Google」を有効化(サポートメールアドレスを選択して保存)

[ ] Work1.3 Cloud Firestoreデータベース作成
    期限: Phase1.4 開始まで
    手順:
      1. Firebaseコンソール > Firestore Database > 「データベースを作成」
      2. ロケーション: asia-northeast1(東京)推奨
      3. 「本番環境モード」で開始(ルールはPhase1.6でAIがデプロイ用ファイルを作成)

[x] Work1.4 GitHubリモートリポジトリの用意 (2026-07-19 完了確認)
    期限: Phase1.1 開始まで(各タスク完了ごとにcommit+pushするルールのため)
    手順:
      1. ターミナルで `gh auth status` を実行し、GitHub CLIが認証済みか確認
         (Claude Codeのセッション内なら「! gh auth status」で実行可能)
      2. 認証済みであればリポジトリ作成(git init/リモート設定/初回push)はAIが実行する
      3. 未認証の場合は `gh auth login` を実行して認証するか、
         GitHub上で空のリポジトリ(例: foot-tactic)を作成しリモートURLをAIに共有する

=====================================================
Work2: AI文字起こし用APIキーの準備(動作検証用)
=====================================================
※OpenAI APIキーは用意できないため取得作業は行わない。
  OpenAI連携(Phase6.3)は実装のみ行い、動作検証・テストはGeminiで実施する(CLAUDE.md参照)

[ ] Work2.1 Google Gemini APIキーの取得
    期限: Phase6.4 開始まで
    手順:
      1. https://aistudio.google.com/ でAPIキーを発行
    備考: 無料枠の範囲で検証可能な想定

=====================================================
Work3: Googleドキュメント保存用のGoogle Cloud設定
=====================================================
[ ] Work3.1 Docs API / Drive APIの有効化とOAuth同意画面の設定
    期限: Phase7.1 開始まで
    手順:
      1. https://console.cloud.google.com/ でFirebaseと同じGCPプロジェクトを開く
      2. 「APIとサービス > ライブラリ」から以下を有効化:
         - Google Docs API
         - Google Drive API
      3. 「APIとサービス > OAuth同意画面」で以下を設定:
         - アプリ名・サポートメール・デベロッパー連絡先
         - スコープに documents / drive.file を追加
         - 公開ステータスが「テスト中」の場合、テストユーザーに自分のGoogleアカウントを追加
    備考: OAuthクライアントIDはFirebase AuthのWebクライアントを流用可能。
          別途必要になった場合はAIが指示する

=====================================================
Work4: リリース関連
=====================================================
[ ] Work4.1 Firebase CLIのログイン認証
    期限: Phase8.5 開始まで
    手順:
      1. ターミナルで `firebase login` を実行しブラウザで認証
         (Claude Codeのセッション内なら「! firebase login」で実行可能)

[ ] Work4.2 OAuth同意画面の本番公開・審査申請(一般公開する場合のみ)
    期限: 一般ユーザーへのリリース判断まで(開発・本人利用のみなら不要)
    手順:
      1. GCPコンソール > OAuth同意画面 > 「アプリを公開」
      2. documentsスコープはセンシティブスコープのためGoogleの審査が必要になる場合がある
    備考: テスト中ステータスのままでも、テストユーザー登録済みアカウントは利用可能

[ ] Work4.3 実機での最終動作確認
    期限: リリース判断まで
    手順:
      1. iPhone(Safari)/Android(Chrome)で本番URLにアクセス
      2. マイク権限の許可、録音→文字起こし、PWAホーム画面追加、
         Googleドキュメント保存を一通り確認
